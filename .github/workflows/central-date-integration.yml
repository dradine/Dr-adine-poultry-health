# DISABLED — stable baseline protection
name: Central Date Integration (DISABLED)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — stable baseline protected"
