# DISABLED — frozen stable baseline ce3952d
# This workflow is intentionally inert. Do not re-enable.
name: Finalize reports hierarchy (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — stable baseline is ce3952d28c74f71726b882b4173ca369306bdefc"
