# DISABLED — stable baseline protection
name: Install Adine performance engine (DISABLED)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — stable baseline protected"
