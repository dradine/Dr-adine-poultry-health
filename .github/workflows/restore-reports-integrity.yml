# DISABLED — stable baseline protection
name: Restore reports.html integrity (DISABLED)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — stable baseline protected"
