# DISABLED — stable baseline protection
name: Strict Farm Score V3 (DISABLED)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — stable baseline protected"
