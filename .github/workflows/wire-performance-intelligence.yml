# DISABLED — stable baseline protection
name: Wire performance intelligence (DISABLED)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — stable baseline protected"
