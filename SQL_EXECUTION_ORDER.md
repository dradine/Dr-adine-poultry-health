# ترتیب اجرای SQL — Benchmark V5.1

## 1) BENCHMARK_V5_CORE.sql
فقط همین فایل برای موتور بنچمارک V5.1 اجرا شود.

این فایل:
- RPC اصلی بنچمارک را می‌سازد.
- حداقل ۱۰ فارم مستقل را الزام می‌کند.
- هر فارم را فقط یک‌بار در cohort وزن می‌دهد.
- یک رکورد نماینده و معتبر از هر فارم انتخاب می‌کند.
- سطح مقایسه را مرحله‌ای انتخاب می‌کند: نوع+ژنتیک+سویه ← نوع+ژنتیک ← نوع.
- در صورت نبود نمونه کافی، بنچمارک را منتشر نمی‌کند.
- صدک مقایسه‌ای و امتیاز مقایسه‌ای ۰ تا ۱۰۰ را برمی‌گرداند.
- روند خود گله را جداگانه برمی‌گرداند.

## 2) FARM_FLOCK_LIFECYCLE_V3.sql
فقط اگر قبلاً نصب نشده است اجرا شود.

## 3) professional_access_compatibility.sql
فقط اگر ساختار دسترسی متخصص پروژه به این سازگارسازی نیاز دارد اجرا شود.

### نکته
`INTERNAL_BENCHMARK_V5.sql` عمداً حذف شده تا دو SQL هم‌نام/هم‌عملکرد باعث اجرای تکراری و سردرگمی نشوند.
