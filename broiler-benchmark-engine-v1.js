/* ADINE POULTRY HEALTH CENTER — BROILER BENCHMARK V1
   Independent flock-level peer benchmark. Read-only. */
(function(global){'use strict';
const METRICS={body_weight:{label:'وزن متوسط',unit:'گرم',dec:0,kind:'context'},weekly_gain:{label:'افزایش وزن هفتگی',unit:'گرم',dec:0,kind:'higher'},weekly_fcr:{label:'FCR هفتگی',unit:'',dec:2,kind:'lower'},cumulative_fcr:{label:'FCR تجمعی',unit:'',dec:2,kind:'lower'},cv:{label:'CV',unit:'٪',dec:1,kind:'lower'},uniformity10:{label:'یکنواختی ±۱۰٪',unit:'٪',dec:1,kind:'higher'},uniformity15:{label:'یکنواختی ±۱۵٪',unit:'٪',dec:1,kind:'higher'},weekly_mortality:{label:'تلفات هفتگی',unit:'٪',dec:2,kind:'lower'},cumulative_mortality:{label:'تلفات تجمعی',unit:'٪',dec:2,kind:'lower'},livability:{label:'زنده‌مانی',unit:'٪',dec:2,kind:'higher'}};
const SCORE_WEIGHTS={weekly_fcr:15,cumulative_fcr:15,weekly_mortality:7,cumulative_mortality:10,livability:8,cv:10,uniformity10:10,uniformity15:5,weekly_gain:20};
function client(){return global.supabaseClient||global.supabase||null}
function fmt(v,d){if(v===null||v===undefined||Number.isNaN(Number(v)))return '—';return Number(v).toLocaleString('fa-IR',{maximumFractionDigits:d,minimumFractionDigits:0})}
function pct(v){return v===null||v===undefined?'—':`P${fmt(Math.round(Number(v)),0)}`}
function positionExplanation(metric,p){if(p===null||p===undefined||Number.isNaN(Number(p)))return '';const n=Math.max(0,Math.min(100,Math.round(Number(p))));if(n===50)return 'تقریباً در میانه جامعه';if(metric?.kind==='context')return n>50?`بالاتر از حدود ${fmt(n,0)}٪ گله‌های مرجع`:`پایین‌تر از حدود ${fmt(100-n,0)}٪ گله‌های مرجع`;if(n>=99)return 'در بالاترین سطح جامعه';if(n>50)return `بهتر از حدود ${fmt(n,0)}٪ گله‌های مرجع`;if(n<=1)return 'در پایین‌ترین سطح جامعه';return `ضعیف‌تر از حدود ${fmt(100-n,0)}٪ گله‌های مرجع`}
function positionLabel(metric,p){const base=pct(p);const explanation=positionExplanation(metric,p);return explanation?`جایگاه: ${base} · ${explanation}`:`جایگاه: ${base}`}
function labelFor(key){return METRICS[key]?.label||key}
function scoreInfo(cohort){const parts=[];let weight=0;Object.entries(SCORE_WEIGHTS).forEach(([key,w])=>{const m=cohort?.metrics?.[key];const p=Number(m?.percentile);if(Number(m?.n)>=10&&Number.isFinite(p)){parts.push({key,w,p:Math.max(0,Math.min(100,p))});weight+=w}});if(!weight)return{score:null,count:0,coverage:0};const score=parts.reduce((s,x)=>s+x.p*x.w,0)/weight;return{score:Math.max(0,Math.min(100,score)),count:parts.length,coverage:weight}}
function scoreLabel(score){if(score===null||score===undefined)return 'داده کافی برای امتیاز نهایی وجود ندارد';const n=Number(score);if(n<40)return 'نیازمند توجه';if(n<55)return 'ضعیف';if(n<70)return 'متوسط';if(n<85)return 'خوب';return 'بسیار خوب'}
function scoreStars(score){if(score===null||score===undefined)return 0;return Math.max(0,Math.min(5,Math.round(Number(score)/20)))}
async function load(flockId,ageDays,opts={}){const sb=client();if(!sb)throw new Error('اتصال به Supabase آماده نیست.');const {data,error}=await sb.rpc('get_broiler_benchmark_v1',{p_flock_id:flockId,p_age_days:ageDays??null,p_age_window_days:opts.ageWindow??3,p_recent_limit:opts.recentLimit??30});if(error)throw error;return data}
function cohort(data,key){return(data?.cohorts||[]).find(x=>x.key===key)||null}
function sampleLabel(n){return n<10?'جامعه ناکافی (<۱۰ گله)':n<20?'بنچمارک اولیه':n<50?'قابل اتکا':'جامعه قوی'}
function render(root,data,state){
 if(!root)return;if(!data?.ok){root.innerHTML=`<div class="bb-error">${data?.message||'داده کافی برای Benchmark وجود ندارد.'}</div>`;return}
 const f=data.flock||{},cohorts=data.cohorts||[],regionNames={north:'شمال',south:'جنوب',east:'شرق',west:'غرب',center:'مرکز'},age=f.age_days??'—';
 root.innerHTML=`<section class="bb-shell" dir="rtl"><div class="bb-head"><div><div class="bb-kicker">BENCHMARK گوشتی آدینه</div><h2>جایگاه گله در جامعه همتا</h2><p>سن ${fmt(age,0)} روز · ${f.strain?`سویه ${f.strain}`:'سویه ثبت نشده'}${f.region?` · منطقه ${regionNames[f.region]||f.region}`:''}</p></div><button class="bb-back" type="button" data-bb-back>بازگشت</button></div><div class="bb-controls"><label>جامعه مقایسه<select data-bb-cohort>${cohorts.map(c=>`<option value="${c.key}" ${c.key===(state?.cohort||'all')?'selected':''}>${c.label}</option>`).join('')}</select></label><label>بازه سنی<select data-bb-window><option value="2" ${state?.ageWindow===2?'selected':''}>±۲ روز</option><option value="3" ${state?.ageWindow===3?'selected':''}>±۳ روز</option><option value="5" ${state?.ageWindow===5?'selected':''}>±۵ روز</option><option value="7" ${state?.ageWindow===7?'selected':''}>±۷ روز</option></select></label></div><div class="bb-summary"><div><span>گله</span><strong>${f.name||'گله انتخاب‌شده'}</strong></div><div><span>سن Benchmark</span><strong>${fmt(age,0)} روز</strong></div><div><span>جامعه</span><strong id="bb-n">—</strong><small id="bb-confidence"></small></div></div><div class="bb-score" data-score-card><div class="bb-score-head"><div><span class="bb-score-kicker">امتیاز نهایی</span><h3>امتیاز عملکرد گله</h3><p>ترکیب صدک‌های شاخص‌های عملکردی Benchmark؛ فقط شاخص‌های دارای جامعه حداقل ۱۰ گله وارد امتیاز می‌شوند.</p></div><div class="bb-score-number"><strong data-score>۰</strong><span data-score-label>در حال محاسبه</span></div></div><div class="bb-gauge" aria-label="امتیاز عملکرد گله"><div class="bb-gauge-ring"></div><div class="bb-gauge-inner"></div><div class="bb-needle" data-score-needle></div><div class="bb-gauge-center"><strong data-score-center>۰</strong><span>از ۱۰۰</span></div></div><div class="bb-stars" data-score-stars aria-label="امتیاز ستاره‌ای"><span>☆</span><span>☆</span><span>☆</span><span>☆</span><span>☆</span></div><div class="bb-score-meta"><span data-score-coverage>—</span><span>وزن‌ها بر اساس اهمیت عملکردی شاخص‌ها تنظیم شده‌اند.</span></div></div><div class="bb-grid">${Object.keys(METRICS).map(k=>`<article class="bb-metric" data-metric="${k}"><div class="bb-metric-title"><span>${labelFor(k)}</span><b data-value>—</b></div><div class="bb-bar"><i data-marker></i></div><div class="bb-statline"><span data-pctl>جایگاه: —</span><span data-range>—</span></div><div class="bb-detail"><span>P10 <b data-p10>—</b></span><span>P25 <b data-p25>—</b></span><span>میانه <b data-med>—</b></span><span>P75 <b data-p75>—</b></span><span>P90 <b data-p90>—</b></span></div></article>`).join('')}</div><div class="bb-note"><b>تفسیر:</b> Benchmark جایگزین استاندارد رسمی یا هدف مدیریتی نیست؛ جایگاه گله را در بین گله‌های واقعی مشابه سیستم نشان می‌دهد. گله‌های فعال و بسته‌شده، در صورت داشتن رکورد معتبر در سن موردنظر، در جامعه محاسبه می‌شوند. در جامعه کمتر از ۱۰ گله، صدک نمایش داده نمی‌شود و نتیجه صرفاً توصیفی است.</div></section>`;
 const c=cohort(data,state?.cohort||'all')||cohorts[0];if(!c)return;let total=0;
 Object.keys(METRICS).forEach(k=>{const m=c.metrics?.[k],el=root.querySelector(`[data-metric="${k}"]`);if(!el)return;if(!m){el.style.display='none';return}el.style.display='block';total=Math.max(total,Number(m.n)||0);el.querySelector('[data-value]').textContent=fmt(m.current,METRICS[k].dec)+(METRICS[k].unit?' '+METRICS[k].unit:'');const enough=Number(m.n)>=10;el.querySelector('[data-pctl]').textContent=enough&&m.percentile!=null?positionLabel(METRICS[k],m.percentile):`${sampleLabel(Number(m.n)||0)}`;el.querySelector('[data-range]').textContent=`${fmt(m.n,0)} گله`;el.querySelector('[data-p10]').textContent=fmt(m.p10,METRICS[k].dec);el.querySelector('[data-p25]').textContent=fmt(m.p25,METRICS[k].dec);el.querySelector('[data-med]').textContent=fmt(m.median,METRICS[k].dec);el.querySelector('[data-p75]').textContent=fmt(m.p75,METRICS[k].dec);el.querySelector('[data-p90]').textContent=fmt(m.p90,METRICS[k].dec);const p=enough?Math.max(0,Math.min(100,Number(m.percentile)||0)):50;el.querySelector('[data-marker]').style.left=`${p}%`;el.dataset.status=m.n<10?'low':m.n<20?'initial':m.n<50?'reliable':'stable'});
 const si=scoreInfo(c),scoreEl=root.querySelector('[data-score]'),scoreCenter=root.querySelector('[data-score-center]'),scoreLabelEl=root.querySelector('[data-score-label]'),needle=root.querySelector('[data-score-needle]'),coverage=root.querySelector('[data-score-coverage]'),stars=root.querySelector('[data-score-stars]');
 if(si.score==null){scoreEl.textContent='—';scoreCenter.textContent='—';scoreLabelEl.textContent=scoreLabel(null);coverage.textContent='شاخص کافی برای امتیازدهی وجود ندارد';needle.style.transform='translateX(-50%) rotate(-90deg)';stars?.classList.remove('is-animating')}else{const target=Math.round(si.score),starCount=scoreStars(target);scoreEl.textContent='۰';scoreCenter.textContent='۰';scoreLabelEl.textContent='در حال ارزیابی…';coverage.textContent=`${fmt(si.count,0)} شاخص از ۹ شاخص وارد امتیاز شده‌اند · پوشش وزنی ${fmt(si.coverage,0)}٪`;needle.style.transform='translateX(-50%) rotate(-90deg)';if(stars){stars.classList.remove('is-animating');stars.querySelectorAll('span').forEach(s=>{s.textContent='☆';s.classList.remove('is-on')});void stars.offsetWidth;stars.classList.add('is-animating')}
 const start=performance.now(),duration=2200;const tick=now=>{const t=Math.min(1,(now-start)/duration),ease=1-Math.pow(1-t,3),value=target*ease;scoreEl.textContent=fmt(value,0);scoreCenter.textContent=fmt(value,0);needle.style.transform=`translateX(-50%) rotate(${-90+(value*1.8)}deg)`;if(stars){const shown=Math.min(starCount,Math.floor(ease*(starCount+0.85)));stars.querySelectorAll('span').forEach((s,i)=>{const on=i<shown;s.textContent=on?'★':'☆';s.classList.toggle('is-on',on)})}if(t<1)requestAnimationFrame(tick);else{scoreEl.textContent=fmt(target,0);scoreCenter.textContent=fmt(target,0);scoreLabelEl.textContent=scoreLabel(target);needle.style.transform=`translateX(-50%) rotate(${-90+(target*1.8)}deg)`;if(stars)stars.querySelectorAll('span').forEach((s,i)=>{const on=i<starCount;s.textContent=on?'★':'☆';s.classList.toggle('is-on',on)})}};requestAnimationFrame(tick)}
 root.querySelector('#bb-n').textContent=fmt(total,0)+' گله';root.querySelector('#bb-confidence').textContent=sampleLabel(total);
}
function mount(root,flockId,ageDays){let state={cohort:'all',ageWindow:3};async function refresh(){root.innerHTML='<div class="bb-loading">در حال محاسبه Benchmark…</div>';try{const data=await load(flockId,ageDays,{ageWindow:state.ageWindow});render(root,data,state);root.querySelector('[data-bb-cohort]')?.addEventListener('change',e=>{state.cohort=e.target.value;refresh()});root.querySelector('[data-bb-window]')?.addEventListener('change',e=>{state.ageWindow=Number(e.target.value)||3;refresh()});root.querySelector('[data-bb-back]')?.addEventListener('click',()=>global.AdineComparisonLanding?.showLanding?.())}catch(e){root.innerHTML=`<div class="bb-error">خطا در Benchmark: ${e?.message||'خطای نامشخص'}</div>`}}refresh();return{refresh}}
global.AdineBroilerBenchmark={load,render,mount,METRICS};})(window);
