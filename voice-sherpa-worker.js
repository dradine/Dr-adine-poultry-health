/* ADINE SHENAVA WORKER 7.7
 * Official Shenava Rizeh v1.0 (32M) + Sherpaw VAD-ASR WASM.
 * OfflineRecognizer is exported by @sherpaw/vad-asr, not @sherpaw/asr.
 */
const SHERPA_ENTRY='https://cdn.jsdelivr.net/npm/@sherpaw/vad-asr@0.0.2/dist/index.js';
const C=Object.freeze({modelUrl:'https://huggingface.co/Reza2kn/Shenava-Rizeh-v1.0-sherpa-onnx/resolve/main/model.onnx?download=true',tokensUrl:'https://huggingface.co/Reza2kn/Shenava-Rizeh-v1.0-sherpa-onnx/resolve/main/tokens.txt?download=true',modelName:'model.onnx',tokensName:'tokens.txt',minBytes:30000000,maxBytes:50000000,sampleRate:16000,tokenCount:1025,importTimeout:30000,wasmTimeout:60000,modelTimeout:90000,sessionTimeout:120000});
let depsPromise=null,modulePromise=null,recognizerPromise=null;
const fail=(code,message)=>{const e=Error(message||code);e.code=code;return e};const post=(id,ok,payload={})=>self.postMessage({id,ok,...payload});const progress=(id,stage,detail)=>self.postMessage({id,type:'progress',stage,detail});const withTimeout=(p,ms,code)=>{let t;return Promise.race([p,new Promise((_,r)=>t=setTimeout(()=>r(fail(code)),ms))]).finally(()=>clearTimeout(t))};
async function getDeps(){if(depsPromise)return depsPromise;depsPromise=(async()=>{try{const api=await withTimeout(import(SHERPA_ENTRY),C.importTimeout,'SHERPA_IMPORT_TIMEOUT');if(typeof api.initVADASRModule!=='function'||typeof api.OfflineRecognizer!=='function')throw fail('SHERPA_API_EXPORT');return api}catch(e){throw e.code?e:fail('SHERPA_IMPORT',e?.message||String(e))}})().catch(e=>{depsPromise=null;throw e});return depsPromise}
async function getModule(){if(modulePromise)return modulePromise;modulePromise=(async()=>{progress(0,'wasm');const{initVADASRModule}=await getDeps();try{const m=await withTimeout(initVADASRModule(),C.wasmTimeout,'WASM_INIT_TIMEOUT');if(!m?.FS_createDataFile)throw fail('WASM_FS_UNAVAILABLE');return m}catch(e){throw e.code?e:fail('WASM_INIT',e?.message||String(e))}})().catch(e=>{modulePromise=null;throw e});return modulePromise}
async function fetchModel(){return withTimeout(Promise.all([fetch(C.modelUrl,{cache:'force-cache',mode:'cors'}),fetch(C.tokensUrl,{cache:'force-cache',mode:'cors'})]).then(async([mr,tr])=>{if(!mr.ok)throw fail(`MODEL_HTTP_${mr.status}`);if(!tr.ok)throw fail(`TOKENS_HTTP_${tr.status}`);const[model,tokens]=await Promise.all([mr.arrayBuffer(),tr.text()]);if(model.byteLength<C.minBytes||model.byteLength>C.maxBytes)throw fail('MODEL_SIZE',`Unexpected Shenava Rizeh model size: ${model.byteLength}`);if(tokens.split(/\r?\n/).filter(Boolean).length!==C.tokenCount)throw fail('TOKEN_COUNT');return{model,tokens}}),C.modelTimeout,'MODEL_LOAD_TIMEOUT')}
async function getRecognizer(id){if(recognizerPromise)return recognizerPromise;recognizerPromise=(async()=>{const Module=await getModule();progress(id,'model');const{model,tokens}=await fetchModel();try{Module.FS_unlink?.(`/${C.modelName}`);Module.FS_unlink?.(`/${C.tokensName}`);Module.FS_createDataFile('/',C.modelName,new Uint8Array(model),true,true,true);Module.FS_createDataFile('/',C.tokensName,new TextEncoder().encode(tokens),true,true,true)}catch(e){throw fail('MODEL_FS',e?.message||String(e))}progress(id,'session');const{OfflineRecognizer}=await getDeps();const cfg={featConfig:{sampleRate:C.sampleRate,featureDim:80},modelConfig:{nemoCtc:{model:`/${C.modelName}`},tokens:`/${C.tokensName}`,numThreads:1,provider:'cpu',debug:0},decodingMethod:'greedy_search'};try{const rec=await withTimeout(Promise.resolve().then(()=>new OfflineRecognizer(cfg,Module)),C.sessionTimeout,'SESSION_INIT_TIMEOUT');if(!rec)throw fail('SESSION_EMPTY');return{rec,Module}}catch(e){throw e.code?e:fail('SESSION_INIT',e?.message||String(e))}})().catch(e=>{recognizerPromise=null;throw e});return recognizerPromise}
async function infer(id,pcm,sampleRate){if(!pcm?.length)throw fail('EMPTY_AUDIO');if(sampleRate!==C.sampleRate)throw fail('BAD_SAMPLE_RATE');const e=await getRecognizer(id);progress(id,'decode');const stream=e.rec.createStream();try{stream.acceptWaveform(C.sampleRate,pcm);while(e.rec.isReady(stream))e.rec.decode(stream);const result=e.rec.getResult(stream);const text=String(result?.text||'').replace(/\s+/g,' ').trim();if(!text)throw fail('EMPTY_TEXT');progress(id,'done',{chars:text.length});return text}catch(err){if(err?.code)throw err;throw fail('DECODE_ERROR',err?.message||String(err))}finally{try{stream.free?.()}catch(_){}}}
self.onmessage=async e=>{const m=e.data||{},id=m.id;try{if(m.type==='preload'){await getRecognizer(id);post(id,true,{text:''});return}if(m.type==='infer'){const pcm=m.pcm instanceof Float32Array?m.pcm:new Float32Array(m.pcm||[]);post(id,true,{text:await infer(id,pcm,m.sampleRate)});return}throw fail('UNKNOWN_WORKER_COMMAND')}catch(err){post(id,false,{code:err?.code||'ASR_WORKER_ERROR',message:err?.message||String(err)})}};
